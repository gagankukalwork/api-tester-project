# API Tester Project

## 📌 Overview
A Node.js based API testing tool with PostgreSQL backend.  
It allows users to send requests (GET, POST, PUT, DELETE) and view request history in a simple UI.

## 🚀 Features
- Test any API endpoint via form
- Save request history in PostgreSQL
- View past requests with status codes and timestamps
- Simple UI built with Express and EJS

## 🛠️ Setup Instructions
1. Clone the repo:
   ```bash
   git clone https://github.com/gagankukalwork/api-tester-project.git
